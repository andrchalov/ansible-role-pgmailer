---
pgmailer_docker_name: pgmailer
pgmailer_docker_image: andrchalov/pgmailer:latest
pgmailer_user: pgmailer
pgmailer_password: ''
pgmailer_loglevel: ERROR

# pgmailer_pghost:
# pgmailer_pgdatabase:
# pgmailer_pguser:
# pgmailer_pgpassword:
# pgmailer_smtp_host:
# pgmailer_smtp_login:
# pgmailer_smtp_password:
# pgmailer_sentry_url:
