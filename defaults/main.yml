---
pgmailer_dir: /srv/pgmailer
pgmailer_docker_image: andrchalov/psycopg2:latest
pgmailer_loglevel: "error"

# pgmailer_pghost:
# pgmailer_pgdatabase:
# pgmailer_pguser:
# pgmailer_pgpassword:
# pgmailer_smtp_host:
# pgmailer_smtp_login:
# pgmailer_smtp_password:
# pgmailer_sentry_url:
